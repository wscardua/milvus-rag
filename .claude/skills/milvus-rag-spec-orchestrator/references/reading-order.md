# Ordem de leitura

1. `docs/specs/state/status.md`
2. feature alvo (`docs/specs/features/`)
3. contratos do frontmatter (`docs/specs/contracts/`)
4. arquitetura relevante (`docs/specs/architecture/`)
5. testes esperados (`docs/specs/testing/`)
