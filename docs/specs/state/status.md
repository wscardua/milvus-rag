# Estado operacional — Milvus RAG (POC)

Memória operacional única e enxuta. Substitui a máquina completa de workflow-runs/changelogs do projeto original.

## Trabalho em aberto

| Item | Feature | Etapa atual | Próxima ação | Status |
|---|---|---|---|---|
| WORK-001 | todas | Infra up; `apps/api/` com camada de dados (SQLAlchemy + Alembic) criada e migrada | Esqueleto FastAPI (`main.py`, routers, schemas, services) | aberto |
| WORK-002 | FEAT-WEB-001 | **POC RAG funcional ponta a ponta**: dados + API (org/upload/links/query) + worker (ingestão/classificação/retry) + frontend Django. Testado com LM Studio real (embedding + chat): upload→ingestão→classificação IA→retrieval→resposta com citações→expansão por vínculos→"sem contexto suficiente" | Refino/robustez: heartbeat periódico no worker, avaliação de retrieval, testes automatizados; FEAT-MCP-001 (servidor MCP) | aberto |
| WORK-003 | FEAT-UPLOAD-001, FEAT-INGEST-001, FEAT-QUERY-001, FEAT-WEB-001 | **Implementado e mergeado** (`update/processo-e-logs`, PR #5 → `9e22809`): (1) exclusão de documento (cascade + Milvus + arquivo), (2) visualizar (modal)/baixar documento, (3) feedback 👍/👎 + `query_log` com métricas, (4) tela Logs & Saúde + `system_log` + `/health` por serviço. ADR-0010/0011, contratos e specs propagados; migration aplicada; smoke test OK; suíte pytest (12 testes) verde | — | concluido |
| WORK-004 | FEAT-MCP-001 | **Implementado e mergeado** (`feature/mcp-query-server`): servidor MCP `apps/mcp/` (FastMCP/stdio, 4 tools cliente HTTP da API — ADR-0005) + endpoint `POST /retrieve` na API (retrieval sem geração). Decisões §14 resolvidas: transporte **stdio**; `retrieve_chunks` via **endpoint dedicado**. Specs/contratos/ADR propagados; bumps FEAT-MCP→0.2.0 e FEAT-QUERY→0.6.0. Testes verdes (API 15 + MCP 6) e smoke test ponta a ponta OK | — | concluido |
| WORK-006 | FEAT-INGEST-001, FEAT-UPLOAD-001 | **Chunking adaptativo por doc_type (ADR-0013)** — `update/adaptive-chunking-by-doc-type`, enviado via `/enviar-pr`. Perfis `size`/`overlap` por `doc_type` (todos por env, fallback global — ADR-0006) + `doc_type` obrigatório no upload (API `Form(...)` + UI Django `required`/validação) + **log detalhado de ingestão** (`IngestStats` no `message`/`context` do `system_log`) + novo `doc_type` **"Proposta Técnica"** (300/60). Sem migration; sem mudança de índice/embeddings. ADR-0013 criado, FEAT-INGEST→0.10.0, FEAT-UPLOAD→0.5.0, contrato/taxonomia atualizados. pytest 33 verde | — | concluido |
| WORK-007 | FEAT-UPLOAD-001, FEAT-INGEST-001, FEAT-WEB-001, FEAT-QUERY-001 | **Implementado** (`update/document-metadata-formats-and-validity`) — lote de 5 melhorias entregue: (1) novos formatos **pptx/.ipynb/imagens** (extração + upload; `.ppt` legado → erro claro); (2) **paginação** funcional em Documentos e Logs (`X-Total-Count`); (3) novo eixo **fase de delivery** (Discovery→Deploy) no `document`; (4) **filtro de processo e de fase** na tela de Documentos; (5) **`valid_until`** com rebaixamento de vencidos no retrieval (ADR-0014). ADR-0014 criado; ADR-0002 revisado (formatos). Migration `d7f3a9c1b204` aplicada. Specs/contratos/arquitetura propagados; pytest **46 verde** (33 + 13 novos) | Enviar via `/enviar-pr` (review + merge) | aberto |
| WORK-005 | FEAT-INGEST-001 | **Vision na ingestão (ADR-0012)** — `update/vision-ingestion`, enviado via `/enviar-pr`: descrição de imagens de PDF/DOCX por LLM vision (LM Studio), best-effort, intercalada no texto antes do chunking. Novo `services/vision.py`; `extract.py` extrai imagens (PDF via PyMuPDF render em DPI, DOCX via relações); config `VISION_*`; sem mudança de schema/índice. Refino de acurácia (prompt estrito + `VISION_MAX_TOKENS=800` + `VISION_RENDER_DPI=200`). Validado ponta a ponta pelo worker no PDF real (alucinação eliminada); pytest 15 verde; pip check limpo | — | concluido |
| WORK-008 | FEAT-INGEST-001 | **Implementado** (`fix/log-llm-unavailable`, enviado via `/enviar-pr`): classificação (`pipeline.py:_apply_classification`) e vision (`vision.py:describe_image`) agora emitem `eventlog.log_event("WARNING", "ingestion", "llm_classification_failed"/"llm_vision_failed", ...)` além do `log.warning` de console, quando o LM Studio falha — evento passa a aparecer no `system_log`/tela Logs & Saúde (ADR-0011). Sem mudança de schema/índice/contrato. FEAT-INGEST→0.11.1. pytest 46 verde | — | concluido |
| WORK-009 | — (infra `ops/`, sem feature de produto) | **Implementado e mergeado** (`update/ops-attu-ui`, PR #11 → `e005b3a`): serviço **Attu** (GUI web do Milvus) adicionado ao `ops/docker-compose.yml` (porta `${ATTU_PORT:-3000}`, aponta para `milvus:19530`, `depends_on: milvus healthy`) + `ops/.env.example` (`ATTU_VERSION`/`ATTU_PORT`); `ops/README.md` atualizado (5 serviços, tabela de portas, seção Attu passa a descrever o serviço do compose em vez do `podman run` manual). `docs/guides/ia-spec-workflow.md` removido (redundante — fluxo já coberto pelo `CLAUDE.md`). Sem mudança de schema/índice/contrato/embeddings; sem ADR. YAML validado estruturalmente (5 services); **`podman compose up -d` não pôde ser validado neste ambiente (podman indisponível)** — validação manual pendente do usuário. | Validar `podman compose up -d` localmente (subir Attu + acessar `http://localhost:3000`) | concluido |
| WORK-010 | FEAT-QUERY-001, FEAT-MCP-001, FEAT-WEB-001, FEAT-UPLOAD-001 | **Implementado, testado e enviado via `/enviar-pr`.** Todas as camadas: núcleo Milvus (`vectorstore.py`/`retriever.py`/`pipeline.py`, ADR-0015 — campo dinâmico `delivery_phase`/`tags`, sem drop/recriação), API/domínio (`GET /tags`, filtro `tags` em `GET /documents`, `PATCH` sincroniza `category_id`/`delivery_phase`/`tags` com o Milvus e `tags` vira editável fora do upload, `python -m app.db.reindex_dynamic_fields`), UI Django (selects/checkboxes em Consulta, Documentos e detalhe), MCP (6 tools de lookup + filtros novos nas tools de consulta). Evidência: suíte completa rodada isolada (2x, worker parado) — **API 78 + MCP 15 = 93 verde**; critérios de aceite §10 de `retrieval-and-answer.md`/`mcp-query-server.md` todos `[x]`; `status_impl` das 3 specs → `implementada`. 3 lacunas pré-existentes fechadas (payload Milvus sem `delivery_phase`/`tags`; UI de Consulta sem select de processo; `tags` não editável fora do upload). 4 lacunas novas registradas conscientemente (Lacunas conhecidas): semântica AND de tags fora de escopo, reindexação do acervo é manual, UI não persiste filtros pós-POST, worker real ativo pode causar corrida ao rodar `pytest` localmente. | — | concluido |

> Status possíveis: `aberto`, `bloqueado`, `concluido`, `cancelado`, `substituido`.

## Status de implementação

| Feature | status_spec | status_impl |
|---|---|---|
| FEAT-UPLOAD-001 | aprovada (v0.7.0) | implementada |
| FEAT-INGEST-001 | aprovada (v0.10.0) | implementada |
| FEAT-QUERY-001 | aprovada (v0.8.0) | implementada |
| FEAT-MCP-001 | aprovada (v0.3.0) | implementada |
| FEAT-WEB-001 | aprovada (v0.4.0) | implementada |

- 2026-07-11 — **WORK-010 — `tags` editável via `PATCH /documents/{id}`** (achado do usuário: campo só existia no upload, sumia no modo edição — `FEAT-UPLOAD-001 → 0.7.0`). `DocumentUpdate` ganhou `tags: list[str] | None`; endpoint normaliza (trim, descarta vazios), `tags=[]` limpa a lista inteira (substitui, não é incremental), e sincroniza o Milvus via `vectorstore.sync_document_fields` (campo dinâmico, ADR-0015 — mesmo mecanismo do fix de `category_id`/`delivery_phase`). Editar `tags` **não** altera `classification_source`. **UI:** form "Ciclo de entrega e tags" no detalhe do documento (`documents/views.py` action `save_delivery` estendida; template ganhou `<input name="tags">`, removida a exibição estática redundante). Contrato `upload-and-metadata` documentado. **5 testes novos** (`test_delivery_metadata.py`: atualiza, limpa, normaliza, não toca `classification_source`, sincroniza+limpa no Milvus) — pytest **78 verde** (73 + 5). **Achado de infra no caminho:** rodar a suíte com o worker real ativo em paralelo causa corrida em testes que simulam "documento já indexado" (o worker reprocessa o upload de teste de verdade e substitui o chunk fake, quebrando a asserção por corrida — não por bug). Corrigido nos testes com `_cancel_pending_job` (marca o `ingestion_job` do upload de teste como `failed` antes da manipulação manual do Milvus) **e** operacionalmente (parar o worker antes de rodar a suíte completa). Suíte confirmada 78/78 com o worker parado.
- 2026-07-11 — **WORK-010 — fix: `PATCH /documents/{id}` deixava o payload do Milvus obsoleto** (achado pelo usuário testando manualmente: editou `delivery_phase` de um documento já indexado e o campo dinâmico não atualizava no Milvus). Análise mostrou 2 campos com o mesmo defeito: `category_id` (declarado, ADR-0007 — bug pré-existente, não causado pelo WORK-010) e `delivery_phase` (dinâmico, ADR-0015 — bug novo). Novo `vectorstore.sync_document_fields(document_id, fields)`: lê os chunks já indexados do documento via `query(output_fields=["*"])` (traz `vector` + payload completo, inclusive campos dinâmicos), aplica só as chaves pedidas (`valor=None` remove o campo — só faz sentido pra dinâmico) e reescreve via `upsert` (Milvus não tem update parcial — upsert pela mesma `chunk_id` substitui a linha inteira). Validado por spike manual antes de generalizar (mesma disciplina do ADR-0015). `documents.py:update_document` monta `milvus_fields` a partir do que veio no PATCH e sincroniza **antes** do commit do Postgres (mesma ordem de segurança do `DELETE`, ADR-0010 — se a sincronização falhar, nada fica inconsistente). `tags` não entrou no escopo desta correção porque não é editável via PATCH hoje (só no upload) — sem endpoint, sem como ficar obsoleta por essa via; fica pronta a mesma função se um dia for editável. **5 testes novos** (`test_vectorstore_dynamic_fields.py` +4: atualiza campo declarado, seta/remove campo dinâmico, no-op sem chunks; `test_delivery_metadata.py` +1: PATCH ponta a ponta com chunk manualmente indexado). **pytest 73 verde** (68 + 5), confirmado em execução isolada (uma execução concorrente acidental causou 1 falha por contenção real no Milvus — não reproduziu isolada, descartada como falso positivo de ambiente). Ambiente local verificado limpo (Postgres 1 doc real do usuário + Milvus 8 chunks correspondentes — não é lixo de teste).
- 2026-07-11 — **WORK-010 — evidência final consolidada, pronto para `/enviar-pr`** (`milvus-rag-test-engineer`): suíte completa (`apps/api` + `apps/mcp`) rodada do zero conforme os comandos do `CLAUDE.md` — **API 68 + MCP 15 = 83 verde**, sem flakiness. Critérios de aceite §10 revisados: os 4 itens `[ ]` (UI filtra `delivery_process`; `/query`/`/retrieve` filtram `delivery_phase`/`tags`; tools de lookup retornam as listas; tools de consulta repassam os filtros novos) viraram `[x]` — cobertura direta por teste automatizado (Milvus real + unitário) ou smoke test manual documentado (UI, sem suíte automatizada Django nesta POC). `status_impl` de FEAT-QUERY-001 e FEAT-MCP-001: `em_andamento` → **`implementada`**. **Lacunas conhecidas em `status.md`**: 3 entradas antigas fechadas com `~~riscado~~ → resolvido` (payload Milvus sem `delivery_phase`; `tags` não era filtro de retrieval; UI de Consulta sem select de processo — achado do review do WORK-004/PR#4); 3 entradas novas registradas conscientemente — semântica **AND** de `tags` fora de escopo (ADR-0015, reavaliar sob demanda), reindexação do acervo é **manual** (`python -m app.db.reindex_dynamic_fields` + worker ativo — documentos antigos só ganham os campos novos depois disso rodar), UI de Consulta não persiste os filtros selecionados após o POST (pré-existente para squad/category/doc_type, os 3 filtros novos herdam a mesma limitação — não é regressão). Nenhum bloqueio encontrado. Próximo: `/enviar-pr`.
- 2026-07-11 — **WORK-010 — tools MCP implementadas** (todas as camadas do WORK-010 concluídas): `apps/mcp/app/client.py` ganhou `list_squads`/`list_delivery_processes(squad_id?)`/`list_categories`/`list_doc_types`/`list_delivery_phases`/`list_tags` (proxy fino dos GETs de `organization-admin`); `list_documents` passou a traduzir `delivery_phase`/`tags` (mesmo nome do contrato, sem transformação — `tags` vai como lista, httpx serializa como múltiplos `?tags=`). `apps/mcp/app/server.py` expõe as 6 tools novas (mesmo padrão try/except ApiError→RuntimeError das existentes) e as docstrings de `search_documents`/`retrieve_chunks`/`list_documents` passaram a documentar os filtros novos e instruir o agente a resolver nome→id com as tools de lookup antes de filtrar `squad`/`delivery_process` (sem isso, filtrar por esses dois é impossível — a API exige UUID). `apps/mcp/README.md`: tabela de tools dividida em "Consulta"/"Lookup (WORK-010)"; removida a nota antiga "`tags` não é filtro de retrieval" (WORK-004) — agora é. **Testes:** `test_client_mapping.py` ganhou 9 casos (tradução de `delivery_phase`/`tags`, as 6 tools de lookup como proxy 1:1 via `@pytest.mark.parametrize`, `list_delivery_processes` com/sem `squad_id`) — MCP **15 verde** (6 pré-existentes + 9). **pytest total do WORK-010: API 68 + MCP 15 = 83 verde**, estável. Todas as camadas implementadas (núcleo Milvus/ADR-0015, API/domínio, UI Django, MCP). Próximo: `milvus-rag-test-engineer` para evidência final consolidada, depois `/enviar-pr`.
- 2026-07-11 — **WORK-010 — UI Django implementada** (`milvus-rag-django-web`): `apps/web/query/views.py` (`consulta`) passou a buscar `GET /delivery-processes`, `GET /delivery-phases` e `GET /tags` (além dos GETs que já buscava) e a montar `filters["delivery_phase"]` (igual aos demais campos escalares) e `filters["tags"]` (lista, via `request.POST.getlist("tags")` — checkboxes, semântica OR); `templates/query/consulta.html` ganhou `<select name="delivery_process">`, `<select name="delivery_phase">` e um bloco de checkboxes de tags (`.tag-checks`/`.tag-check`, CSS novo em `app.css`) exibido só quando há tags no acervo (`{% if tags %}`). `apps/web/documents/views.py` (`document_list`) e `templates/documents/list.html` ganharam `<select name="tags">` (1 tag, mesmo padrão auto-submit dos filtros existentes — usa `GET /documents?tags=`). **Smoke test manual** (curl, API reiniciada pra pegar as rotas novas): `GET /tags` (`[]` vazio, depois `["smoke-test-tag"]` após upload de teste), `/consulta/` e `/` (Documentos) renderizando `delivery_process`/`delivery_phase`/`tags` nos formulários; checkbox de tag confirmado aparecendo dinamicamente; documento de teste removido ao final. Sem suíte de testes automatizados no Django (não existe nesta POC). Próximo: tools MCP de lookup + filtros novos, depois `milvus-rag-test-engineer`.
- 2026-07-11 — **WORK-010 — domínio/endpoints da API (Fase 2) implementados** (`milvus-rag-fastapi-domain`): `apps/api/app/api/documents.py` ganhou `GET /tags` (`select(func.unnest(Document.tags)).distinct()`, ordenado em Python) e o parâmetro `tags: list[str] | None = Query(None)` em `GET /documents`, filtrando com `Document.tags.overlap(tags)` (mesma semântica OR do Milvus, usa o índice GIN já existente — ADR-0007/0015). `apps/api/app/schemas/query.py`: comentários atualizados — `filters` já era `dict | None` solto, então `tags` como lista não exigiu novo campo Pydantic. Nova rotina `apps/api/app/db/reindex_dynamic_fields.py` (`python -m app.db.reindex_dynamic_fields`): enfileira um `ingestion_job` novo (`state=pending`) para cada documento cujo estado atual (via `document_status`, o `ingestion_job` mais recente) é `indexed` — recomendado/gradual, não bloqueante, precisa do worker rodando para processar. **Testes:** `tests/test_tags_endpoint.py` (4 casos — `GET /tags` distinct/ordenado, filtro por 1 tag, filtro por 2+ tags como OR, `GET /documents` sem `tags` continua funcionando). **Achado no caminho:** o fixture de integração do Milvus (`test_vectorstore_dynamic_fields.py`) tinha `flush()` só após o `upsert` — o `delete()` do teardown também precisa de `flush()`, senão os chunks de teste ficam "fantasma" nas execuções seguintes (16 chunks órfãos encontrados e limpos manualmente antes da correção). **pytest 68 verde** (64 + 4), estável em 2 execuções consecutivas da suíte completa. Próximo: `milvus-rag-django-web` (Fase 1 + Fase 2 na UI).
- 2026-07-11 — **WORK-010 — núcleo de RAG (Fase 2, ADR-0015) implementado** (`milvus-rag-embeddings-retrieval`): spike técnico rodado contra o Milvus real (`ops/`, v2.5.4) — upsert com campo dinâmico (`delivery_phase`, `tags` serializada `",a,b,"`) + `search` com `filter` de igualdade e `LIKE` sobre eles, confirmando o caminho principal do ADR (sem drop/recriação da coleção). `apps/api/app/services/vectorstore.py`: `_build_filter_expr` extraído do `search()` (função pura, testável sem Milvus), `serialize_tags`, `_escape_tag_for_like` (sanitiza `,`/`%`/aspas no valor da tag antes do `LIKE`); `upsert_chunks` aceita `delivery_phase`/`tags` opcionais (omitidos quando o documento não tem valor, não gravados como vazio). `apps/api/app/domain/retrieval/retriever.py`: `_FILTER_MAP` ganhou `delivery_phase`; `_map_filters` trata `tags` como lista (passa adiante, não normaliza como escalar). `apps/api/app/domain/ingestion/pipeline.py`: linha de `upsert_chunks` inclui os 2 campos condicionalmente. **Testes:** `tests/test_retrieval_filters.py` (13 unitário — `_map_filters`, `_build_filter_expr`, escaping, semântica OR) + `tests/test_vectorstore_dynamic_fields.py` (5 integração real — igualdade exclui chunk sem campo; 1 tag; múltiplas tags OR; sem filtro não exclui; combinação `delivery_phase`+`tags`). **pytest 64 verde** (46 pré-existentes + 18 novos), sem regressão. Pendente: `GET /tags`/filtro de tags em `GET /documents` (fastapi-domain), UI (django-web), tools MCP, reenfileiramento do acervo (recomendado, não bloqueante).
- 2026-07-11 — **WORK-010 — estratégia de testes definida** (`milvus-rag-test-strategy`, docs-first, antes do código): §11 de `retrieval-and-answer.md`/`mcp-query-server.md` ganharam cenários concretos — unitário (`_map_filters` com `tags` viram OR de `LIKE`; escaping; tools MCP como proxy 1:1), integração (`/query`/`/retrieve` filtrando por `delivery_phase`/1 e 2+ `tags` contra Milvus real), fluxo (agente resolve nome→id via lookup antes de filtrar; UI com `delivery_process` selecionado), avaliação de RAG (documento com 1 de N tags pedidas ainda aparece — semântica OR; documento sem payload novo não quebra busca geral), regressão (filtros pré-existentes squad/category/doc_type isolados e combinados; rebaixamento por vigência ADR-0014 continua ativo; as 4 tools MCP existentes continuam retrocompatíveis sem os novos filtros) e o **spike técnico do ADR-0015** como pré-requisito da Fase 2 antes de generalizar `vectorstore.py`/`retriever.py`. Nenhuma pendência nova — pronto para as skills técnicas de implementação. Próximo: `milvus-rag-fastapi-domain` + `milvus-rag-django-web` + `milvus-rag-embeddings-retrieval`, depois `milvus-rag-test-engineer`.
- 2026-07-11 — **WORK-010 — ADR-0015 revisado e aceito** (docs-first, antes do código): a v1 do ADR-0015 assumia que era preciso dropar/recriar a coleção Milvus + reindexar o acervo inteiro como evento único para adicionar `delivery_phase`/`tags` ao payload. O `milvus-rag-software-architect` encontrou que a coleção já é criada com `enable_dynamic_field=True` (`vectorstore.py:_ensure_collection`), rodando Milvus 2.5.4/pymilvus 2.5.4 (suporte maduro a campo dinâmico desde 2.2+) — dá para gravar os 2 campos como dinâmicos nos upserts novos e filtrar via `expr` normalmente, **sem** drop/recriação nem reindexação obrigatória (reenfileirar o acervo vira recomendado/gradual, não bloqueante para liberar a feature). O `milvus-rag-architecture-guard` validou que a fronteira continua correta (filtro só na FastAPI; Django/MCP só mandam `filters` como dict; `GET /tags`/filtro de tags em `GET /documents` via SQLAlchemy ORM parametrizado, sem SQL cru). ADR-0015 reescrito com essa decisão e movido de `proposta` para **`aceita`** — ressalva registrada: falta um spike técnico rápido (não bloqueia a decisão) confirmando `LIKE` sobre campo dinâmico nesta versão do Milvus; se falhar, plano B é o schema declarado + drop/recriação da v1, já documentado como fallback. `retrieval-and-answer.md`/`mcp-query-server.md` (§6/§13/§14) atualizados para refletir "recomendado, não bloqueante" em vez de "reindexação obrigatória". Próximo: `milvus-rag-test-strategy`.
- 2026-07-11 — **WORK-010 — specs preparadas** (docs-first, antes do código): `retrieval-and-answer.md` (FEAT-QUERY-001) → **0.8.0** — escopo dos filtros `delivery_phase`/`tags` (semântica OR, string delimitada no Milvus), nota de payload vazio para documentos não reindexados, 2 critérios de aceite `[ ]` novos; `mcp-query-server.md` (FEAT-MCP-001) → **0.3.0** — 6 tools de lookup (`list_squads`/`list_delivery_processes`/`list_categories`/`list_doc_types`/`list_delivery_phases`/`list_tags`), contrato `organization-admin` adicionado às dependências. Contrato `query-and-citations` ganhou a semântica de `filters.delivery_phase`/`filters.tags` (a menção a `tags` já existia no contrato **sem nunca ter sido implementada** — corrigido agora para casar com a implementação real prevista). Contrato `organization-admin` ganhou seção "Listas de apoio" (`GET /doc-types`, `GET /delivery-phases`, `GET /tags` — os 2 primeiros já existiam sem contrato formal; `/tags` é novo). **ADR-0015 criado** (`proposta`) — extensão do payload Milvus com `delivery_phase` (igualdade) e `tags` (OR via string delimitada + `LIKE`, alternativa mais simples que um campo `ARRAY` nativo não validado nesta versão do Milvus); drop/recriação da coleção + reenfileiramento de `ingestion_job=pending` para todo o acervo; revisa parcialmente o ADR-0014 (que havia decidido deixar `delivery_phase` fora do Milvus por falta de caso de uso — caso de uso surgiu agora). Próximo: `software-architect`/`architecture-guard` validam o ADR (principalmente o padrão `LIKE`) antes de virar `aceita` e entrar em implementação.
- 2026-07-11 — **WORK-010 aberto** (`update/retrieval-filters`, tipo `update`): completar os filtros de retrieval por `squad`/`delivery_process`/`delivery_phase`/`tags` na UI de Consulta e nas tools do servidor MCP — hoje só `squad`/`category`/`doc_type` funcionam ponta a ponta. Branch a partir de `origin/main` (`298c7b2`). Escopo em duas fases, um WORK só (para reindexar o acervo uma única vez): **Fase 1** — `delivery_process` já filtra no Milvus (`_PAYLOAD_FIELDS`/`_FILTER_MAP`) e a view já lê `request.POST.get("delivery_process")`, mas falta o `<select>` no template `consulta.html`; MCP ganha tools `list_squads`/`list_delivery_processes`/`list_categories`/`list_doc_types` (proxy fino de `GET /squads`, `/delivery-processes`, `/categories`, `/doc-types` já existentes) para o agente resolver nome→UUID antes de filtrar. Sem ADR. **Fase 2** — `delivery_phase` (hoje só filtra `GET /documents` no Postgres, nunca chegou ao Milvus) e `tags` (hoje só gravado no Postgres, sem endpoint de leitura nem filtro nenhum) entram no payload do Milvus — schema da coleção é fixo após criada, então exige **ADR** + drop/recriação da coleção + reenfileiramento de `ingestion_job=pending` para todo o acervo já ingerido; inclui `GET /tags` novo, filtro de tags em `GET /documents` via índice GIN existente, `<select name="delivery_phase">` + campo de tag na UI, tools MCP `list_delivery_phases`/`list_tags`. Specs afetadas: `retrieval-and-answer.md` (FEAT-QUERY-001) e `mcp-query-server.md` (FEAT-MCP-001) — conteúdo e bump de versão durante o trabalho/no `/enviar-pr`.
- 2026-07-11 — **WORK-009 enviado via `/enviar-pr` e mergeado** (`update/ops-attu-ui`, PR #11 → `e005b3a`, branch remoto deletado). Review de consistência (sem código de aplicação alterado): diff conferido, sem descasamento entre docs/specs/`status.md`; nenhum achado crítico. `main` local sincronizada com `origin/main`. Pendência: validação funcional de `podman compose up -d` fica para a máquina do usuário (sem `podman` no ambiente de execução).
- 2026-07-11 — **WORK-009 implementado**: `ops/README.md` atualizado (5 serviços, tabela de portas, seção Attu passa a descrever o serviço do compose). Sem `podman` disponível no ambiente de execução para validar `podman compose up -d` — YAML checado estruturalmente (5 services); validação funcional fica pendente para a máquina do usuário. Próximo: validar local + `/enviar-pr`.
- 2026-07-11 — **WORK-009 aberto** (`update/ops-attu-ui`, tipo `update`): branch a partir de `origin/main` (`5734fe6`). Escopo: (1) integrar o **Attu** (GUI web do Milvus) ao `ops/docker-compose.yml` + `ops/.env.example` como serviço do compose (`ATTU_VERSION`/`ATTU_PORT`), no lugar do `podman run` manual hoje documentado em `ops/README.md`; (2) remover `docs/guides/ia-spec-workflow.md` (guia redundante — sem referências restantes no repo, conteúdo já coberto pelo fluxo descrito no `CLAUDE.md`). Mudança de infra local (`ops/`), sem impacto em schema/índice/contrato/embeddings — nenhuma spec de feature ou ADR previsto. Trabalho recuperado de um stash pré-existente (`git stash pop`).
- 2026-07-11 — **WORK-008 implementado e enviado via `/enviar-pr`** (`fix/log-llm-unavailable`): classificação (`pipeline.py:_apply_classification`) e vision (`vision.py:describe_image`) passam a chamar `eventlog.log_event("WARNING", "ingestion", "llm_classification_failed"/"llm_vision_failed", ...)` quando o LM Studio falha — antes só logavam via `logging.warning` (console), sem chegar ao `system_log`/tela Logs & Saúde. Sem mudança de schema/índice/contrato; sem ADR novo. `ingestion-pipeline.md` → 0.11.1 (§3 + Histórico). pytest 46 verde.
- 2026-07-11 — **WORK-008 aberto** (`fix/log-llm-unavailable`, tipo `fix`): branch a partir de `origin/main` (`79bc17d`). Escopo: os dois caminhos best-effort que usam LLM na ingestão — classificação (`pipeline.py:_apply_classification`) e descrição de imagens/vision (`vision.py:describe_image`) — degradam silenciosamente quando o LM Studio falha, logando só via `logging.warning` (console); nunca chamam `eventlog.log_event`, então a falha não aparece no `system_log` nem na tela Logs & Saúde (ADR-0011), ao contrário do que já acontece em `/query`/`/retrieve` e no worker. Ajuste: adicionar `eventlog.log_event(...)` nesses dois pontos. Sem mudança de schema/índice/contrato. Spec afetada: FEAT-INGEST-001 (`ingestion-pipeline.md`) — conteúdo e bump de versão serão feitos durante o trabalho/no `/enviar-pr`.
- 2026-07-10 — **WORK-007 implementado** (`update/document-metadata-formats-and-validity`): 5 itens. **Docs-first:** ADR-0014 (fase de delivery + `valid_until` no schema + rebaixamento por vigência no retrieval) criado; ADR-0002 revisado (+PPTX/`.ipynb`/imagens); taxonomy §3 (lista fechada de fases); bumps FEAT-UPLOAD→0.6.0, INGEST→0.11.0, QUERY→0.7.0, WEB→0.5.0; contratos `upload-and-metadata`/`query-and-citations`/`logs-and-health` + `database.md` propagados. **API:** `document` ganha `delivery_phase`/`valid_until` (migration `d7f3a9c1b204`, ambos nullable, sem backfill); `extract.py` extrai PPTX (`python-pptx` — slides/notas/tabelas + imagens via vision), `.ipynb` (markdown+código+saídas) e imagens (vision, fallback = nome do arquivo); `.ppt` legado → `PermanentIngestionError` claro. `retriever._demote_expired` multiplica o score de vencidos por `retrieval_expired_score_factor` (novo config, default 0.5) e reordena — vale p/ `/query` e `/retrieve`. `GET /documents` e `GET /logs` paginam com `X-Total-Count`; `/documents` filtra por `delivery_phase`; novo `GET /delivery-phases`. `PATCH` aceita `delivery_phase`/`valid_until` **sem** marcar `classification_source`. **UI Django:** paginação (Documentos+Logs), filtro de processo e fase na listagem, campos fase/vigência no Upload/Detalhe (form próprio `save_delivery`), colunas fase/vigência, `accept` do upload estendido. **Dep:** `python-pptx==1.0.2` (+Pillow); `pip check` limpo (só aviso `grpcio` pré-existente). **pytest 46 verde** (33 + 13 novos: metadados/validação/filtro/paginação/rebaixamento/extração). Próximo: `/enviar-pr`.
- 2026-07-10 — **WORK-007 aberto** (`update/document-metadata-formats-and-validity`, tipo `update`): lote de 5 melhorias de documentos/retrieval. Branch a partir de `origin/main` (`44dbec2`). Escopo: **(1)** aceitar novos formatos na ingestão/upload — **ppt/pptx** (python-pptx), **.ipynb** (notebook Jupyter) e **imagens** como documento próprio (reusa vision, ADR-0012) → FEAT-UPLOAD-001 + FEAT-INGEST-001; **(2)** **paginação** nas telas de Documentos e Logs → FEAT-WEB-001 + contratos `upload-and-metadata`/`logs-and-health` (limit/offset ou page) + API; **(3)** novo eixo **fase de delivery** no documento (`Discovery | Refinamento Funcional | Refinamento Técnico | Desenvolvimento | Testes | Release | Deploy`), distinto de `delivery_process` → schema `document` (migration) + ADR-0007/taxonomia + FEAT-UPLOAD/WEB; **(4)** **filtro de processo** (`delivery_process`) na tela de Documentos → FEAT-WEB-001 (fecha lacuna conhecida: API já suporta, faltava select); **(5)** campo **`valid_until`** no documento para rebaixar/filtrar documentos vencidos no retrieval (vigência) → schema (migration) + FEAT-QUERY-001 (estratégia de retrieval — gatilho de ADR) + FEAT-UPLOAD/WEB. Nenhuma mudança de modelo/dimensão/métrica de embeddings prevista. Ajustes de conteúdo das specs, bumps de versão e ADR(s) acontecem durante o trabalho e no `/enviar-pr`.
- 2026-07-10 — **WORK-006 enviado via `/enviar-pr`** (`update/adaptive-chunking-by-doc-type`, PR #8): commit → PR → `/code-review` → merge. Entrega: chunking adaptativo por `doc_type` (ADR-0013) + `doc_type` obrigatório no upload + log detalhado de ingestão + `doc_type` "Proposta Técnica". **Review (2 fixes antes do merge):** (1) `chunking.py` ganhou **fail-fast** no import — todo slug mapeado em `_DOC_TYPE_TO_SLUG` precisa ter `chunk_size_<slug>`/`chunk_overlap_<slug>` em `Settings`, senão `RuntimeError` (evita cair silenciosamente no default global reportando `perfil=doc_type`); (2) `pipeline.py` reusa `token_counts[ordinal]` em vez de re-`split()` por chunk. pytest 33 verde; `alembic check` sem migration; `pip check` limpo (só aviso `grpcio` pré-existente).
- 2026-07-10 — **WORK-006 — novo doc_type "Proposta Técnica" (ADR-0013)**: adicionado à taxonomia (`reference/taxonomy.md` §2) e ao `DOC_TYPES` (habilita UI via `/doc-types` + validação 422). Perfil de chunking **300/60** (deck de slides com tabelas — overlap 20% para preservar tabelas na fronteira; próximo da Apresentação, um pouco acima no overlap). Sem migration; sem mudança de índice. `config.py`/`.env.example`/`_DOC_TYPE_TO_SLUG`/teste de perfis atualizados; tabela do ADR-0013 estendida.
- 2026-07-10 — **WORK-006 aberto** (`update/adaptive-chunking-by-doc-type`, tipo `update`): **chunking adaptativo por doc_type (ADR-0013)**. Branch a partir de `origin/main` (`38f40d0`). Perfis `size`/`overlap` por `doc_type` configuráveis via env (`CHUNK_SIZE_<SLUG>`/`CHUNK_OVERLAP_<SLUG>`, ADR-0006) com **fallback global** (`chunk_size_words`/`chunk_overlap_words`) para `Outro`/`None`/não-mapeado + **`doc_type` obrigatório no upload** (API `Form(...)` + UI Django `required`/validação na view). Banco permanece `Mapped[str | None]` (sem migration — regra na API); `DocumentOut.doc_type` permanece `str | None` (retrocompat). Sem mudança de índice Milvus/contrato de embeddings. Docs-first: **ADR-0013** criado; **FEAT-INGEST-001→0.10.0**, **FEAT-UPLOAD-001→0.5.0**; contrato `upload-and-metadata` (doc_type obrigatório + 422). Obs.: os identificadores ADR-0012/WORK-005 do prompt original já estavam ocupados pela feature de vision — remapeados para **ADR-0013/WORK-006**. **+ Log detalhado de ingestão (ADR-0011):** `ingest_document` retorna `IngestStats` e o worker registra no console + evento `job_indexed` (`system_log.context`) o resumo do processamento — doc_type, perfil aplicado (específico/fallback), texto (chars/palavras), size/overlap, chunks, tokens/chunk (min/avg/max), vetores, vision e `duration_ms`.
- 2026-07-10 — **WORK-005 — review (PR #7)**: 3 correções antes do merge. **(1)** Caminho vision do PDF/DOCX agora é **best-effort de fato** — falha do PyMuPDF (ou da extração com vision) **degrada para texto** (pypdf/python-docx) em vez de virar `PermanentIngestionError`; guard por página no PDF. **(2)** `_image_png_for_vision` **sempre retorna PNG** (converte o bitmap embutido, incl. CMYK→RGB) — casa com o `data:image/png` enviado ao LM Studio. **(3)** **Dedup por `xref`**: imagem repetida (ex.: logo em várias páginas) reusa a descrição, sem nova chamada LLM (posição por página preservada). Testes 15 verdes.
- 2026-07-10 — **WORK-005 — refino de acurácia (ADR-0012)**: 3 ajustes contra alucinação do vision em tabelas/prints densos, após validação com PDF real (o `gemma-3-4b` inventava conteúdo inexistente — ex.: "AWS Lambda/S3/API Gateway" numa retrospectiva Miro). **(1)** Prompt estrito de fidelidade em `vision.py` (transcrever só o visível; `[ilegível]` em vez de adivinhar) — eliminou a fabricação. **(2)** `VISION_MAX_TOKENS` default 300→800 (tabelas não truncam). **(3)** novo `VISION_RENDER_DPI` (default 200): `extract._image_png_for_vision` renderiza a **região da imagem na página** em alta DPI (`get_image_rects`+`get_pixmap(clip,dpi)`) no lugar do bitmap embutido — fallback para o embutido. Antes/depois no PDF real: alucinação AWS sumiu, tabelas 4-6 completas e com GPON/datas corretos. Resíduo (erro de OCR em células densas do modelo 4B) segue como limite do modelo — troca por `.env`. Testes 15 verdes; sem nova dep.
- 2026-07-10 — **WORK-005 aberto + implementado** (`update/vision-ingestion`, tipo `update`): **Vision na ingestão — ADR-0012**. Branch a partir de `origin/main` (`7ca057b`). **(API)** novo `app/services/vision.py` — `describe_image(image_bytes, filename, position)` reusa `lmstudio.client` (mesmo base_url/api_key do chat), modelo por `VISION_MODEL`, `max_tokens` por `VISION_MAX_TOKENS`, `temperature=0`, chamada OpenAI-vision (`image_url` base64); **best-effort → `""`** em falha. `extract.py`: `_extract_pdf`/`_extract_docx` extraem imagens embutidas (PDF via **PyMuPDF/fitz**, texto ainda via pypdf; DOCX via relações `python-docx`) e **intercalam** `[Imagem — página N: …]` / `[Imagem — parágrafo N: …]` antes do chunking quando `VISION_ENABLED` (default `true`); `extract_text(path, filename) -> str` **inalterada**. `pipeline.py`: só `log.info` quando vision ligado. Config `VISION_ENABLED`/`VISION_MODEL`/`VISION_MAX_TOKENS` (ADR-0006). **Dep:** `pymupdf==1.26.7` (**AGPL** — trade-off aceito p/ POC, registrado no ADR-0012); `pip check` limpo (aviso `grpcio` é pré-existente). **Sem mudança de schema/índice/embeddings.** Spec FEAT-INGEST→0.9.0; §3/§9/§13/§15 propagados. Próximo: `/enviar-pr` (descrições contra LLM real exigem LM Studio ativo).
- 2026-07-09 — **WORK-004 — review (PR #6)**: 2 correções antes do merge. (1) MCP deixou de **anunciar o filtro `tags`** (docstring de `search_documents` + README) — o retrieval não filtra por `tags` (Milvus não indexa), então o agente recebia resultados amplos achando que filtrou. (2) `client._request` passou a **envolver `resp.json()`** — corpo 200 não-JSON agora vira `ApiError` (mantém o contrato "falhas viram ApiError") em vez de exceção crua. Achados menores registrados nas Lacunas (chunks abaixo do limiar em `/retrieve`; `top_k=0` falsy-zero; query DB extra no caminho insuficiente; duplicações client/endpoint).
- 2026-07-09 — **WORK-004 concluído** (`feature/mcp-query-server`): implementação de FEAT-MCP-001 enviada via `/enviar-pr` (commit → PR → review → merge). Testes verdes (API 15 + MCP 6) e smoke test ponta a ponta (MCP → API viva: 4 tools + caminho "API fora do ar"). `.claude/settings.json` incluído no PR a pedido (allowlist de permissões).
- 2026-07-09 — **WORK-004 implementado** (`feature/mcp-query-server`): **FEAT-MCP-001**. **(API)** novo `POST /retrieve` — retrieval puro sem geração (`retriever.retrieve_chunks` refatorado a partir da base de `answer_query`); schemas `Retrieve*`; não grava `query_log`. **(MCP)** app `apps/mcp/app/` — `server.py` (FastMCP, transporte `stdio`, 4 tools `search_documents`/`retrieve_chunks`/`list_documents`/`get_document`), `client.py` (cliente HTTP + tradução de filtros + `ApiError` → erro explícito ao agente), `config.py` (`API_BASE_URL` default 8001). `requirements.txt`/`.env.example`/`README`. **Dep:** `mcp==1.12.4` no venv compartilhado exigiu fixar `sse-starlette==3.0.3` + `starlette<0.42` (versões novas do SDK puxam starlette≥0.49, que conflita com fastapi 0.115.6) — `pip check` limpo. Removido `from __future__ import annotations` do `server.py` (FastMCP inspeciona anotações reais). **ADR-0005** refinado (endpoint dedicado + stdio + porta 8001); contrato `query-and-citations` +seção "Retrieval puro"; `backend-api.md`/`mcp-server.md` propagados; bumps FEAT-MCP→0.2.0 (implementada) e FEAT-QUERY→0.6.0.
- 2026-07-09 — **WORK-004 aberto**: início de `feature/mcp-query-server` (tipo `feature`) — implementação de **FEAT-MCP-001** (servidor MCP de consulta ao acervo, spec já aprovada v0.1.0). Branch criado a partir de `origin/main` (`9e22809`). Decisões travadas para as pendências §14 da spec: **(1) transporte `stdio`** como default (HTTP/Streamable configurável por env fica como evolução — POC local, restrita à rede local); **(2) `retrieve_chunks` via endpoint dedicado** (`POST /retrieve`, retrieval sem geração) em vez de flag no `POST /query` — mantém os contratos coesos. A (2) estende a API (FEAT-QUERY-001) → prevê bump de versão e possível nota de ADR ao expor o retrieval já existente em `domain/retrieval`. WORK-003 marcado como `concluido` (mergeado no PR #5).
- 2026-07-09 — **WORK-003 implementado** (`update/processo-e-logs`): 4 itens. **(1) Exclusão** — `DELETE /documents/{id}` (Milvus→Postgres cascade chunk/job/link→arquivo) + botão no Detalhe. **(2) Arquivo** — `GET /documents/{id}/file` (inline/attachment, RFC 5987) + modal Visualizar (PDF/TXT/MD/HTML) e Baixar, via proxy Django. **(3) Feedback** — `query_log` grava **toda** consulta com métricas (scores, modelos, chunk params, latency); `POST /query` devolve `query_id`; `POST /query/{id}/feedback` (👍/👎) + botões na Consulta. **(4) Logs & Saúde** — `system_log` (API+worker via `eventlog.log_event`); `/health` por serviço (PG/Milvus/LM Studio/worker) + fila; `/logs` com filtros; nova tela Django (7ª) + heartbeat leve no worker. **ADR-0010** (ciclo de vida do documento) e **ADR-0011** (observabilidade) criados; contratos `upload-and-metadata`/`query-and-citations` estendidos + novo `logs-and-health`; `database.md`/`vector-index.md`/`backend-api.md`/`system-overview.md` propagados; bumps FEAT-UPLOAD→0.4.0, INGEST→0.8.0, QUERY→0.5.0, WEB→0.4.0 (aprovada). Migration `cc34b9227367` aplicada. **Smoke test** (API+proxy): delete cascade, file inline/attachment, query_log+feedback (204/422/404), /health, /logs, document_deleted — OK.
- 2026-07-09 — **WORK-003 — review (PR #5)**: 2 correções antes do merge. (1) Proxy de arquivo (Django) passou a **repassar falhas** da API (status ≥400 → 404) em vez de servir o corpo de erro como se fosse o arquivo (HTTP 200 corrompido). (2) `/health` do Milvus agora usa `vectorstore.ping()` (somente-leitura, **não cria** a coleção) em vez de `_c()` — health check deixou de ter efeito colateral e passa a sinalizar "coleção ausente". Achados não críticos registrados nas Lacunas (worker "down" em ingestão longa; proxy bufferiza arquivo; `/health` chama LM Studio a cada poll; `since` de `/logs` naive).
- 2026-07-09 — **WORK-003 — testes**: primeira suíte automatizada do repo. `pytest` adicionado (`apps/api/requirements.txt`); `apps/api/tests/` com integração via FastAPI TestClient contra o stack real — ciclo de vida do documento (file inline/attachment, 404, delete cascade chunk/job, reexclusão 404), query_log+feedback (query_id, métricas, rating 204/422/404, pergunta vazia 422) e observabilidade (/health por componente + fila, /logs com filtros level/component). **12 passed.** Comando documentado no CLAUDE.md.
- 2026-07-09 — **WORK-003 aberto**: início de `update/processo-e-logs` (tipo `update`) — melhorias de **processo** e **logs**. Branch criado a partir de `origin/main` (`e5c62db`).
- 2026-07-09 — **PR**: consolidação da POC RAG no branch `feature/frontend-layout` — specs (ADR-0007/0008/0009, contratos, taxonomia, arquitetura), `apps/api` (FastAPI + worker) e `apps/web` (Django). Abre via `/enviar-pr`.
- 2026-07-09 — **Implementação (núcleo RAG — worker + `/query`)**: services `lmstudio`/`embeddings`/`llm`/`vectorstore` (Milvus 768/COSINE/HNSW); `domain/ingestion` (extração PDF/DOCX/HTML/XLSX/txt-md-py, chunking por palavras, classificação IA restrita à taxonomia, pipeline idempotente); `app/worker.py` (claim SKIP LOCKED + retry/backoff + visibility timeout — ADR-0009); `domain/retrieval` + `POST /query` (busca vetorial c/ filtros + expansão de 1 salto por vínculos + geração com citações + `linked_flow` + limiar "sem contexto suficiente"). **Testado com LM Studio real** (`text-embedding-embeddinggemma-300m` + `gemma-3-4b-it-qat`): upload→indexação→classificação (título/categoria/resumo via IA)→consulta com resposta ancorada e citação; expansão trouxe definição de doc vinculado; pergunta fora do acervo → insufficient_context. Consulta Django renderiza resposta/citações/fluxo. FEAT-UPLOAD/INGEST/QUERY/WEB → status_impl `implementada`. (setuptools pinado <81 p/ pymilvus.)
- 2026-07-09 — **Implementação (Frontend Django)**: criado `apps/web/` (Django 5.1, cliente HTTP da API — sessão/mensagens por cookie, sem banco de domínio, guardrail respeitado). Cliente `core/client.py`; views `organization` (Squads/Processos CRUD), `documents` (listagem c/ filtros, upload multipart, detalhe c/ edição de classificação + selects dependentes + vínculos/fluxo), `query` (shell tolerante ao `/query` ausente). Layout Carbon (`static/css/app.css`, `templates/base.html` + 6 páginas). API ganhou `/doc-types` e `/link-types`. Testado ponta a ponta pela UI (squad→processo→upload→listagem→detalhe→PATCH override; consulta sinaliza `/query` indisponível). Comandos em CLAUDE.md.
- 2026-07-09 — **Implementação (API parte 1)**: `apps/api/app/` ganhou `main.py` (FastAPI + `/health`), `schemas/` (Pydantic = contratos), `services/storage.py` e routers `organization.py` (CRUD squad/processo + taxonomia), `documents.py` (upload enfileira `ingestion_job`, listagem c/ filtros, detalhe, `PATCH` de overrides → `classification_source=user`) e `links.py` (vínculos tipados, validação de mesma squad). Testado ponta a ponta via HTTP (happy path + 422 mesma-squad/doc_type, 409 RESTRICT/duplicado, 415 extensão). `query`/services/worker pendentes.
- 2026-07-09 — **Implementação (camada de dados)**: criado `apps/api/` (ADR-0003) com `config.py` (pydantic-settings, ADR-0006), `db/models.py` (squad, delivery_process, category, subcategory, document, document_link, chunk, ingestion_job), Alembic (migration inicial `78d0b3b785b3`) aplicada no Postgres do `ops/`, e seed idempotente da taxonomia (`app.db.seed_taxonomy` — 7 categorias/30 subcategorias). Deps instaladas no `venv/` (`apps/api/requirements.txt`).
- 2026-07-09 — Taxonomia: novo `doc_type` **"Base de Conhecimento"** (`reference/taxonomy.md` + mock).
- 2026-07-09 — **ADR-0009** (retry/visibility-timeout do worker): política de tolerância a falhas do daemon de ingestão — heartbeat + recuperação de jobs presos em `processing` (embutida na query de claim), retry com backoff exponencial para falhas transitórias (`WORKER_MAX_ATTEMPTS` default 3; timeout default 5min), `failed` imediato para falhas permanentes. `ingestion_job` ganha `attempts`/`started_at`/`heartbeat_at`/`available_at`. FEAT-INGEST-001 → v0.7.0; `database.md` propagado. Complementa ADR-0004.
- 2026-07-09 — **ADR-0008** (vínculos entre documentos + expansão de retrieval): tabela `document_link` (auto-relação direcionada e tipada — `esclarece`/`complementa`/`precede`/`substitui` — restrita à mesma squad); retrieval faz expansão de **1 salto** (inclui esclarece/complementa/precede, exclui `substitui` obsoleto) e a resposta traz `linked_flow[]`. Novo contrato `document-links`; `query-and-citations` +`linked_flow`. **ADR-0007 ajustado**: `title` entra no conjunto sugerido-pela-IA e vira **opcional** no upload. Bumps: FEAT-UPLOAD-001 → v0.3.0, FEAT-INGEST-001 → v0.6.0, FEAT-QUERY-001 → v0.4.0, FEAT-WEB-001 → v0.3.0. Propagado em `database.md`, `taxonomy.md` (tipos de vínculo), contratos e mock.
- 2026-07-09 — **Taxonomia definida**: `reference/taxonomy.md` criado (fonte da verdade) — 7 categorias × 4 subcategorias + 11 `doc_type`. Vira seed de `category`/`subcategory` e lista fechada da classificação por IA. Ligado em ADR-0007, FEAT-INGEST-001 e FEAT-WEB-001.
- 2026-07-09 — **ADR-0007** (organização Squad/Processo + classificação): novas tabelas `squad`, `delivery_process`, `category`, `subcategory`; `document` ganha `delivery_process_id` (NOT NULL, RESTRICT), `category_id`/`subcategory_id`/`summary`/`classification_source`/`ingested_at`; payload Milvus += `squad_id`/`delivery_process_id`/`category`/`subcategory`. Propagado em `database.md`, `vector-index.md`, contratos (`upload-and-metadata` +`PATCH`, `query-and-citations` +filtros, novo `organization-admin`). **FEAT-INGEST-001 → v0.5.0** (passo de classificação por IA). FEAT-WEB-001 → v0.2.0.
- 2026-07-09 — **WORK-002 aberto**: nova feature **FEAT-WEB-001** (Layout do Frontend, UI Django) — 6 telas no IBM Carbon Design System (Documentos, Upload, Detalhe, Consulta, Squads, Processos). Introduz modelo **Squad → Processo de Delivery → Documento** e classificação **sugerida por IA e editável** (`category`/`subcategory`/`summary`). Spec `frontend-layout.md` (rascunho v0.1.0) e mock estático em `mocks/frontend-layout.html` criados. Depende de: ADR de schema (a abrir), extensão de contratos e bump de FEAT-INGEST-001.
- 2026-07-09 — `/enviar-pr` refatorado para **gate único de autorização**: resumo antes de alterar qualquer coisa e uma aprovação que cobre todos os passos até o merge (sem re-confirmar); trava de segurança mantida para achados críticos no review.
- 2026-07-09 — Adicionado slash command `/enviar-pr` (`.claude/commands/`): orquestra o fluxo de PR (sync → atualizar specs/docs → branch/commit → PR → review → merge → sync), derivando a descrição do diff quando não informada.
- 2026-07-09 — **ADR-0006** (parametrização: tudo por env, config única, POC local → gerenciados). Persistência dos containers migrada de volumes nomeados para **bind mounts no projeto** (`data/volumes/`, via `DATA_DIR`), separada de `data/uploads/`. Stack recriada e validada (todos healthy, dados gravando em `data/volumes/`). ADR-0003, backend-api, ops/README e .gitignore atualizados.
- 2026-07-09 — **ADR-0004** (ingestão assíncrona: worker daemon + fila `ingestion_job` no Postgres, embeddings em lote) e **ADR-0005** (MCP como cliente HTTP da API). Nova feature **FEAT-MCP-001**; FEAT-INGEST-001 → v0.4.0 (worker). Estrutura (ADR-0003) ganhou `apps/api/app/worker.py` e `apps/mcp/`; arquitetura e overview propagados.
- 2026-07-09 — **ADR-0003** (estrutura de diretórios: apps/api domínio, apps/web cliente, Alembic/SQLAlchemy, data/uploads). **`ops/` criado**: `docker-compose.yml` (Postgres + Milvus + etcd + minio) com volumes nomeados persistentes, `.env.example` e README com comandos Podman.
- 2026-07-09 — **ADR-0002 revisado**: embeddings migrados para `embeddinggemma-300m` (768, COSINE) servido pelo LM Studio (`/v1/embeddings`); backend deixa de usar `sentence-transformers`. **Postgres + Milvus** passam a subir juntos em containers (runtime **Podman**) via `compose` em `ops/`. Features INGEST/QUERY → v0.3.0; vector-index, arquitetura e overview propagados.
- 2026-07-09 — WORK-001 aberto. Stack decidida e registrada em **ADR-0002** (embeddings locais `bge-m3` 1024/COSINE, geração via LM Studio, formatos PDF/DOCX/TXT-MD/HTML/.py/XLS, Milvus standalone Docker). `vector-index.md` fixado; features migradas para **v0.2.0** e `status_spec: aprovada`; overview, contratos e arquitetura propagados.
- 2026-07-09 — Templates `features/_TEMPLATE.md` e `decisions/_TEMPLATE-ADR.md` criados; 3 features migradas para o template completo (v0.1.0) com Histórico de atualizações próprio.
- 2026-07-09 — Estrutura inicial de specs da POC criada (produto, arquitetura, contratos, features, testes, ADR-0001).

## Lacunas conhecidas

- **Rodar `pytest` (apps/api) com o worker real (`python -m app.worker`) ativo em paralelo pode causar corrida** em testes que fazem upload via `client.post("/documents", ...)`: o upload cria um `ingestion_job=pending` de verdade, e o worker (polling 5s) pode reivindicá-lo durante a execução da suíte, reprocessando o documento de teste "de verdade" (classificação por IA via LM Studio, chunks reais no Milvus) por cima do que o teste esperava manipular manualmente. Mitigado nos testes que simulam "documento já indexado" com `_cancel_pending_job` (`test_delivery_metadata.py`), mas o risco genérico continua para qualquer teste de upload — recomendação operacional: **parar o worker antes de rodar a suíte completa** localmente. `CLAUDE.md` não lista o worker como pré-requisito de `pytest`, então isso é específico de rodar os dois processos ao mesmo tempo por conveniência de teste manual — FEAT-UPLOAD-001/FEAT-INGEST-001.
- Calibrar valores de chunk_size/overlap por doc_type com avaliação de retrieval (golden set de perguntas + recall/precisão) — os defaults são estimativas razoáveis, não valores medidos — FEAT-INGEST-001, ADR-0013.
- **Nomenclatura "tokens" na ingestão** (review WORK-006): `Chunk.token_count` e `IngestStats.tokens_min/avg/max` são contagem de **palavras** (`str.split()`), não tokens do modelo de embeddings; o log operacional ("tokens/chunk") herda o termo. Renomear para "palavras" (ou tokenizar de fato) para não confundir na calibração — FEAT-INGEST-001.
- **Qualidade da descrição de imagens (ADR-0012)** — validado ponta a ponta com PDF real de 13 páginas. **Mitigado** com 3 ajustes (prompt estrito de fidelidade + `VISION_MAX_TOKENS` 800 + `VISION_RENDER_DPI` 200): a alucinação de conteúdo inexistente foi eliminada e as tabelas passaram a ser transcritas completas/corretas. **Resíduo:** o modelo default `gemma-3-4b-it-qat` ainda comete erros de OCR em nomes/células densas (ex.: `Radakian→"Carlton"`) — limite do modelo 4B, aceitável p/ POC. Para máxima fidelidade em tabelas críticas, trocar `VISION_MODEL` por modelo maior/tuned p/ documento (só `.env`) — FEAT-INGEST-001.
- Calibrar limiar de similaridade COSINE para "sem contexto suficiente" — FEAT-QUERY-001 (o `query_log` agora fornece scores/rating/latency para essa calibração).
- Calibrar `retrieval_expired_score_factor` (default 0.5) — fator de rebaixamento de documentos vencidos no retrieval; valor estimado, não medido. Considerar também expor a vigência (`valid_until`/vencido) nas citações da UI — FEAT-QUERY-001, ADR-0014.
- ~~Também no Milvus: `delivery_phase`/`valid_until` **não** entram no payload (filtro de fase é no Postgres; vigência é pós-busca). Se surgir necessidade de filtrar por fase no `/query`, exigirá reindexação para popular o payload — FEAT-QUERY-001, ADR-0014.~~ → **resolvido parcialmente (WORK-010, ADR-0015): `delivery_phase` agora filtra `/query`/`/retrieve` via campo dinâmico no Milvus, sem reindexação obrigatória (recomendada/gradual via `python -m app.db.reindex_dynamic_fields`).** `valid_until`/vigência segue como estava (pós-busca, rebaixamento — ADR-0014, não é filtro).
- Definir limite máximo de tamanho de upload — FEAT-UPLOAD-001.
- ~~Decidir se `query_log` entra na POC~~ → **resolvido (ADR-0011, WORK-003): entra, gravando toda consulta com métricas + feedback.**
- **`system_log` sem retenção/rotação** — a tabela cresce indefinidamente; definir política de expurgo/limite (WORK-003, ADR-0011).
- Definir o **prompt de classificação/resumo** que restringe a saída da IA à taxonomia (`reference/taxonomy.md`) — FEAT-INGEST-001.
- Detalhar payloads/erros dos contratos estendidos na implementação (`organization-admin`, `PATCH` de overrides).
- ~~**Filtro `tags` não é filtro de retrieval** — o contrato `query-and-citations` ainda lista `tags` entre os filtros de `/query`, mas o Milvus não indexa `tags`... e `_map_filters` o ignora. Alinhar o contrato (remover `tags` ou indexá-lo no Milvus) — FEAT-QUERY-001/FEAT-MCP-001 (WORK-004).~~ → **resolvido (WORK-010, ADR-0015): `tags` agora é filtro real de `/query`/`/retrieve`/`GET /documents`/tools MCP, semântica OR, via campo dinâmico no Milvus + `overlap` no Postgres (GIN).**
- **`tags` não é editável via `PATCH /documents/{id}`** — só é definida no upload (`DocumentUpdate` não inclui `tags`). Por isso `vectorstore.sync_document_fields` (WORK-010, fix do PATCH) não precisou cobrir `tags`: sem endpoint de edição, não há como ficar obsoleta por essa via. Se um dia `tags` virar editável no detalhe do documento, o `PATCH` precisa ganhar a mesma sincronização já implementada para `category_id`/`delivery_phase` — FEAT-UPLOAD-001.
- **Semântica AND para `tags`** — o filtro de `tags` (`/query`, `/retrieve`, `GET /documents`, tools MCP) só suporta **OR** (documento com qualquer uma das tags pedidas); AND (documento com todas) ficou fora de escopo do WORK-010, decisão registrada no ADR-0015. Reavaliar se surgir demanda — não exige mudança de schema, só trocar `or`/`overlap` por `and`/`contains` nas duas camadas (Milvus `LIKE` + Postgres `@>`).
- **Reindexação do acervo (WORK-010, ADR-0015) é manual, não roda sozinha** — documentos indexados **antes** desta mudança só ganham `delivery_phase`/`tags` no payload do Milvus depois que alguém rodar `python -m app.db.reindex_dynamic_fields` (enfileira `ingestion_job=pending` para todo documento `indexed`) **com o worker ativo** (`python -m app.worker`) para processar a fila. Até lá, filtrar por `delivery_phase`/`tags` nesses documentos antigos simplesmente não os encontra (busca sem esses filtros continua normal — ADR-0015, "recomendado, não bloqueante"). Nenhum agendamento automático foi criado — decisão consciente da POC.
- **UI de Consulta não persiste os filtros selecionados após o POST** (`squad`/`delivery_process`/`category`/`doc_type`/`delivery_phase`/`tags` voltam desmarcados na resposta renderizada) — comportamento pré-existente para os filtros antigos (squad/category/doc_type), não é regressão do WORK-010, mas os 3 filtros novos herdam a mesma limitação. Melhoria futura: reter os valores enviados no `request.POST` ao re-renderizar o form.
- **Achados de review (PR #6, WORK-004) — follow-ups não críticos (POC):**
  - `/retrieve` (`retrieve_chunks`): retorna os trechos mesmo quando `insufficient_context=true` (difere de `/query`, que zera as citações abaixo do limiar). É intencional (primitivo p/ o agente montar o prompt); consumidores devem checar o flag. Revisar se deve filtrar abaixo do limiar.
  - `top_k=0` é tratado como ausente (`payload.top_k or default`) em `/query` e `/retrieve` — pré-existente; se `0` precisar ter significado, validar com `Field(ge=1)` ou `is None`.
  - Refatoração de `retriever._search`: carrega os chunks antes do teste de limiar, então o caminho "sem contexto" faz um `SELECT ... IN (())` extra (antes era pulado). Só eficiência; mover o carregamento para depois do guard.
  - Duplicações menores (DRY): `client.query`/`client.retrieve` idênticos salvo o path; as 4 tools repetem `try/except ApiError→RuntimeError`; `/query` e `/retrieve` repetem preâmbulo (validação + top_k + 502/eventlog); `RetrieveRequest`≡`QueryRequest`. `httpx.Client` recriado a cada chamada (sem reuso de conexão no servidor stdio de vida longa).
- **Achados de review (PR #4) — follow-ups não críticos (POC):**
  - Worker: heartbeat do **job** (`ingestion_job.heartbeat_at`) ainda não é renovado durante o processamento (só no claim); doc que leve > `WORKER_VISIBILITY_TIMEOUT` pode ser reivindicado em paralelo. Implementar renovação periódica (thread/tick). Pipeline é idempotente → risco é retrabalho, não corrupção. (WORK-003 adicionou `worker_heartbeat` no `system_log` para liveness do daemon no `/health`, mas **não** renova o heartbeat do job — segue aberto.)
  - Upload via API com `links[]` inválidos: `IntegrityError`/`ValidationError` no commit não são tratados → 500 + arquivo órfão em `data/uploads/`. Envolver em try/except e limpar o arquivo. (A UI não envia `links` no upload.)
  - Embeddings enviados num único lote; documentos muito grandes podem exceder o limite do LM Studio. Fatiar em sub-lotes.
  - ~~Consulta (UI): filtro `delivery_process` é suportado pela API mas não há select na tela; adicionar select dependente de squad.~~ → **resolvido (WORK-010): `<select name="delivery_process">` adicionado.** Não ficou dependente de squad (lista todos os processos, sem filtrar pelo squad já selecionado no form) — mesma simplicidade dos demais selects da tela; considerar select dependente como melhoria futura se o número de processos crescer.
  - UI: erros 422 da API (detail em lista) aparecem como repr cru; formatar mensagem amigável. Detalhe redireciona se qualquer GET secundário falhar (acoplamento); ícone de `warning` usa ℹ️.
  - Reindexação: embora embeddings venham antes do delete, delete(Milvus)+upsert não são transacionais com o Postgres; falha entre eles deixa janela até o próximo retry.

## Mapa de integração

- Django (`apps/web/`) → FastAPI (`apps/api/`): contratos `upload-and-metadata` (+file/delete), `query-and-citations` (+feedback), `organization-admin`, `document-links`, `logs-and-health`. Arquivo de documento servido por **proxy** Django.
- FastAPI → PostgreSQL: `squad`, `delivery_process`, `category`, `subcategory`, `document`, `document_link`, `chunk`, `ingestion_job`, `query_log`, `system_log`.
- FastAPI → Milvus: coleção de vetores (`vector[768]` + `chunk_id` + payload `document_id`/`doc_type`/`tags`/`author`/`squad_id`/`delivery_process_id`/`category`/`subcategory`). Container (`ops/`, Podman).
- FastAPI → PostgreSQL: container (`ops/`, Podman).
- FastAPI → LM Studio (API OpenAI-compatível, `base_url` configurável): embeddings `embeddinggemma-300m` (`/v1/embeddings`) e geração (`/v1/chat/completions`).
- Worker (`apps/api/app/worker.py`) → fila `ingestion_job` (Postgres) + LM Studio (embeddings em lote) + Milvus (indexação).
- MCP (`apps/mcp/`) → API FastAPI (`POST /query`, `POST /retrieve`, `GET /documents[...]`) como cliente HTTP (stdio; `API_BASE_URL` default 8001).
