# Servidor MCP — cliente HTTP da API FastAPI (Python 3.10, venv/ compartilhado na raiz)
#
# venv/ é compartilhado com apps/api (FastAPI). O SDK mcp puxa sse-starlette/starlette;
# as versões novas exigem starlette>=0.49, que conflita com fastapi 0.115.6 (starlette<0.42).
# Por isso sse-starlette e starlette são fixados no range compatível com a API — não bumpe
# sem alinhar com apps/api/requirements.txt (usamos só o transporte stdio; SSE fica p/ evolução).
mcp==1.12.4
sse-starlette==3.0.3
starlette>=0.40,<0.42
httpx==0.28.1
pydantic-settings==2.7.1
