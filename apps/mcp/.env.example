# Servidor MCP (ADR-0005/0006) — cliente HTTP da API FastAPI
# Onde a API está no ambiente. Dev local: 8001 (a UI Django usa a 8000).
API_BASE_URL=http://localhost:8001
API_TIMEOUT_SECONDS=60

# Transporte MCP: stdio (default, integração local) ou http/sse (evolução futura)
MCP_TRANSPORT=stdio
